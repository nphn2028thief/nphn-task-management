enum EPath {
  HOME = "/",
  IMPORTANT = "/important",
  COMPLETED = "/completed",
  INCOMPLETED = "/incompleted",
  SIGN_IN = "/sign-in",
}

enum EAPI_URL {
  TASKS = "/api/tasks",
}

export { EPath, EAPI_URL };
