enum EPath {
  HOME = "/",
  IMPORTANT = "/important",
  COMPLETED = "/completed",
  INCOMPLETED = "/incompleted",
  FOLDERS = "/folders",
  SIGN_IN = "/sign-in",
}

enum EAPI_URL {
  TASKS = "/api/tasks",
  TREES = "/api/trees",
}

export { EPath, EAPI_URL };
